import {createElement} from 'react'
import RootLayout from '../RootLayout'
import HomePage from '../pages/HomePage'


const render = {
    rootLayout: createElement(RootLayout),
    homePage: createElement(HomePage),
}

export default render